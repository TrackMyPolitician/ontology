apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.+'
    testCompile 'org.apache.jena:apache-jena-libs:3.+'
}

sourceSets {
    test.java.srcDirs = ['test']
}
